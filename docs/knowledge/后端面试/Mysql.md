# SQL

## inner join 和 in 的区别？

- `select * from 表名 for update;` 是干什么的？
- 数据库如何实现乐观锁？
- MySQL 中有哪些常见函数？（类型转换、字符串、日期、聚合函数）
- char 与 varchar 的区别？
- MySQL 分页中 limit 如何优化？
- 行列转换怎么做？
- union 与 union all 的区别？

# 事务

- 什么是事务？
- 事务的特点（ACID）分别是什么？
- 什么是脏读、不可重复读、幻读？
- 事务的隔离级别有哪些？MySQL 默认是哪种？
- 怎么开启事务？
- JDBC 中怎么控制事务？

# 内连接与外连接

- 内连接与外连接（左连接、右连接）有什么区别？
- group by 和 having 的使用场景？

# Statement 与 PreparedStatement

- 二者有什么区别？为什么 PreparedStatement 更推荐？

# 数据库设计

- 什么是数据库的三范式？
- 各范式的要求是什么？举例违背范式的情况。

# 索引与数据结构

- B 树与 B+ 树的区别？
- 为什么 B+ 树比 B 树更适合作为索引？
- 什么是聚簇索引与非聚簇索引？
- 聚簇索引与非聚簇索引在查询时有什么区别？
- 你知道 B+ 树的叶子结点存放哪些内容吗？
- 索引有哪些类型？
- 什么情况下适合建索引？哪些情况不适合？

# 存储引擎

- InnoDB、MyISAM、Memory 三种引擎的区别？
- 各自支持的特性（事务、锁、外键、索引结构）？

# MySQL 优化

- SQL 优化的常见思路有哪些？
- explain 执行计划怎么看？
- 索引失效的常见原因有哪些？
- 如何优化慢 SQL？
- 页面查询很慢时，你会从哪些层面排查？

# 主从与分布式

- 什么是读写分离？原理是什么？
- MySQL 主从复制的过程？
- 异步复制与半同步复制的区别？
- 什么是分库分表？
- 哪种情况适合分库？哪种情况适合分表？

# 日志

- redo log、binlog、undo log 的区别与作用？

# 锁机制

- MySQL 中有哪些锁类型？（表锁、行锁、页锁、间隙锁、临键锁）
- 意向锁是什么？为什么需要？
- 乐观锁与悲观锁的区别？
- InnoDB 的行锁是怎么实现的？
- 锁等待、死锁如何分析与排查？
- RR 隔离级别下为什么会有间隙锁？
- 如何查看、释放锁？

# MVCC

- 什么是 MVCC？
- MVCC 解决了哪些问题？
- MVCC 是怎么实现的？（undo log、read view 等）
- 当前读与快照读的区别？

# 执行流程

- MySQL 的 SQL 执行流程是怎样的？（连接器 → 分析器 → 优化器 → 执行器）

---

主子要我接下来把这些问题的答案也按你那种“狼风笔记”风格写出来吗？  
我建议从“事务 + 锁 + MVCC”这一整块开始写，正好能顺着讲。
