1、认证
①、认证流程
②、客户端与服务端会话的维持
A、cookie B、session C、session复制 D、粘性session E、jwt D、共享session 
③、 jwt
A、构成headerpayload(载荷）签名（防止篡改)
B、优点：数据带到客户端，服务端不做任何的存储
c、缺点：①、携带数据量多，生成的jwt就会越长
②、续期问题：jwt一旦生成之后就定了。
双token：使用的token (时间比较短)
刷新token----》换token（当使用的token到期的时候，使用刷新token换新的token)
④、共享session (使用redis来存储)

2、授权
菜单的呈现 (不用讲)
授权的实现：
①、基于url地址:
已知：A、用户操作url地址B、用户能访问哪些地址
②、基于annotation :
自定义注解放在方法上的
已知：A、当前执行的方法---》拿到方法上的注解（执行该方法需要什么权限）
B、当前用户能有哪些权限

问题：1、有不有用过自定义注解？
①、权限
②、动态数据源的切换
2、本地缓存在哪些地方用过？
①、权限
3、大数据量的导入
①、可能有大数据量的导入
②、在工作中有不有遇到一些问题？怎么问题最终如何解决的？
③、多线程（在项目中有不有用到过多线程？D

